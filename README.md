# Signal Logic

Rhythmic, antifragile operating system for signal observation, governance, and execution.

This repository contains the executable kernel (`signal_core`), the physics substrate (`rhythm_os`), and the archival doctrine layer (`signal_light_press`).

Documentation and canon live alongside code, but are treated as non-executable memory.
